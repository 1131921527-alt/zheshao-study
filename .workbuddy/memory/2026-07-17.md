# 2026-07-17 工作日志

## 雅思每日15词自动化
- 自动化任务触发，生成 **Day 10**（15词，含记忆故事）
- 脚本：`scripts/gen_ielts_day.py` 正常运行，无报错
- Git push 成功，2 files changed
- CloudStudio 部署完成（完整站 + ielts专用站）
- 排查手机打不开问题：微信内置浏览器拦截 app.codebuddy.work 域名，需用手机自带浏览器

## 全站 JS 语法巡检 + 按钮修复
- 写了 Node 校验脚本（提取所有 HTML 内联 `<script>` 用 `new Function` 语法检查 + `node --check` 独立 .js），全站 63 HTML / 80 内联块 / 3 .js 一次跑完
- 发现并修复 **ielts/day01–04.html** 音频按钮失效（JS 整块崩溃→按钮点不动）：
  - day01：`playWord` 多 `});` 和 `}`；`playSent` 多 `)`
  - day02：`playWord` 结尾有 `}&type=${type}`; 模板残留 + 多余 `}`
  - day03：`playWord`/`playSentence` 结尾有 `&type=${type}`; 模板残留 + 垃圾行 `const a=new Audio(url); a.play(); }`；另 **时钟注入块**破损（缺 `lcCopyWx` 闭合 `};`，直接 `})();` 导致 `Unexpected token ')'`）
  - day04：`story` 字段含未转义半角引号 `"这怎么可能爬得上去"`、`"拿数据说话"`、`"偏斜方向"` 等，破坏 JSON 式数据；统一用正则把"中文里未转义半角引号"换成全角 `""`
  - 源文件 `腾讯龙虾的成品/雅思词汇/ielts-vocab-day01.html` 已不存在，所以 day01–04 是旧模板产物、重跑 build.py 不会覆盖，直接修产物即可
- 修复后全站校验 **0 错误**（按钮全部可绑定/点击）
- git push main（commit 7934451），GitHub Pages 自动重建，线上 day01–04 音频按钮恢复
- CloudStudio 部署本次超时（sandbox 未就绪），未出预览链接

## 用户文件规整体检（15:43 规整后）
- 用户把 `E:\workbuddyFIle` 顶层散落目录与"腾讯龙虾成品"各项目做了一次规整归类
- 全面体检 zheshao-study 网站：
  - 全站 JS 语法校验：63 HTML / 80 内联块 / 3 .js → **0 错误**（按钮可点）
  - 精准资源断链扫描（src/href/url/fetch）：79 引用，**0 真实断链**（4 个误报均为 JS 模板变量/${a.href}/正则）
  - 绝对路径引用检查：仅 ai-prompt/day04.html 文章示例文字里 4 处 `E:\...`，非真实引用，无害
  - 本地服务器实测：首页+各子页+css/png/mp3/manifest/sw 全部 HTTP 200；ielts 音频 445 个 mp3 全在 `ielts/audio/`
  - 结论：**规整未破坏网站，无错误需修复**
- 更新发布：本地=远程 main（7934451）已一致，GitHub Pages 线上即最新；CloudStudio 部署连续 3 次 sandbox 未就绪（本会话级环境问题），转 GitHub Pages 交付，蓝色预览链接待沙箱恢复后重试

## 每日 AI 动态播报自动化（20:55）
- 搜索近 24h 全球 AI 动态，精选 **8 条**：Kimi K3（全球最大开源模型）/ Thinking Machines Inkling（开放权重）/ OpenAI 无屏AI音箱 / 智谱 ARR 破10亿 / Anthropic IPO估值9650亿 / 世界人工智能合作组织WAICO成立（上海）/ DeepSeek 估值超3500亿 / 谷歌推迟 Gemini 3.5 Pro
- 写入 `scripts/ai-news-today.json`（脚本默认路径，此前不存在）
- `python scripts/gen_ai_news.py` 生成 `ai-news/ai-news-2026-07-17.html`，同步至 `03-学习网站/AI动态播报/`（源）与 `zheshao-study/ai-news/`（部署）两处
- git push 成功，commit `25cb153`，main `7934451..25cb153`
