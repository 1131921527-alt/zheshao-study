# 2026-07-21 工作日志

## 1. 删除全站财经内容
- 用户要求：把财经内容删掉，不需要任何财经内容。
- 已删除 `index.html` 中的：财经科普入口卡片、知识中心财经展开项、3 篇财经长文（通胀/复利/基金股票）、SLUG 里的 fin 项、const FIN 整块、renderK 的 fin:FIN、地理长文小结的过渡句「聊完地，下一组聊钱——财经。」、知识中心副标题里的「财经」。
- 同步更新 `manifest.webmanifest` 描述，去掉「财经科普」。
- 验证：全站已无「财经/财经科普/FIN/fin-inflation」残留。
- 注：`src/`、`scripts/` 里有 gen_articles.py / gen_more3.py / rewrite_articles.py 等生成脚本仍含财经模板（休眠源码，不参与部署）。如需彻底清理可后续处理。

## 2. 雅思单词新增 500+ 词
- 用户要求：雅思单词再增加 500 个词。
- 词库 `ielts/ielts_bank.json` 原 489 词（其中仅 195 在 day01-13 展示，294 个原词库闲置）。
- 新增 662 个全新词（脚本 `scripts/add_ielts500.py`，含 word/ipa/pos+zh/story/tip/en/cn），词库总数 → 1151。
- 循环运行 `scripts/gen_ielts_day.py` 64 次，生成 day14~day77（共 77 天页面，约 1155 词槽位，0 重复）。
- `ielts/index.html` 期数更新为「共 78 期」。
- 首页 `index.html` 的 `const IELTS=[...]` 重新生成为「完整版 + Day 1..Day 77」（78 项），知识中心雅思列表可直达。
- 音频：新词若无预录 mp3，模板 `tpl_day.html` 的 playWord 会走浏览器 TTS 兜底（仍可发音）。
- 未运行 `build.py`（源目录缺失，运行会清空列表，铁律不变）。

## 3. 提交并部署（收尾）
- commit `7288571`：69 文件变更（+39903 / -117），已 `git push origin main`（80debb1 → 7288571）。
- 暂存时精准 add 站点文件，刻意排除 `.workbuddy/`（agent 内部记忆，不入站点仓库）。CRLF 警告无影响。
- 线上核验：首页「财经」grep = 0；词库 1151 词；ielts 列表「共 78 期」；day 文件 77 个。
- 线上地址：https://1131921527-alt.github.io/zheshao-study/ （GitHub Pages 约 1-2 分钟构建生效）

## 4. 修正首页雅思词数显示 (用户抓到 489 未同步)
- 用户反馈：新增那么多词，首页还显示 489。
- 根因：首页 3 处写死旧数字（当时词库恰好 489，约定=词库总量）。
- 已修正为 1151（词库 JSON 实际总量）：
  - `index.html:7` meta description「雅思单词489词」→1151
  - `index.html:193` 统计卡 `<div class="n">489</div>` →1151
  - `ielts/full.html:47` 副标题「共 489 词」→1151
- 其余「489」出现处：`.workbuddy` 日志（历史记录，保留）、`ai-prompt/day03.html` 的 `#ec4899`（颜色码，非词数，忽略）。
- 词数核对：词库 JSON=1151；day 页面已发布去重=1065（77天×15，0重复）。展示口径沿用原约定=词库总量。
- commit `4300b27` 已推送 main。

## 5. 知识类卡片重排 (用户截图反馈)
- 用户截图指出：历史占第一格、科技前沿孤零零在最后（右边空位）。
- 要求：AI 科技前沿移到第一，历史移到最后，填满 2 列网格。
- 已调整三处同步：
  - 首页 `quick-grid` 7 张 qcard 顺序：科技前沿→文学→地理→心理学→雅思→运动→历史
  - 知识中心 `s-know` 展开区 section-title 同序重排
  - 副标题 `.sub` 文字同步更新
- commit `30d0434` 已推送 main。

## 5b. 首页网格再精简为 6 张 (用户纠正: 要正好6个)
- 用户纠正：不要 7 张，要「正好 6 个」，填满 3×2 无空位。
- 改动：从首页 `quick-grid` 移除「历史」qcard（保留 `s-know` 知识中心里的历史入口，仍可进）。
- 最终首页网格 6 张顺序：科技前沿→文学→地理→心理学→雅思→运动健康。
- 副标题 `.sub` 同步去掉「历史」。
- commit `b3ce98c` 已推送 main。

## 待办/注意
- 首页 IELTS 列表现在 78 项，较长但可滚动。
- 财经休眠源码未删，介意可后续清理。
