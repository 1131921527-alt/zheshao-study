# 2026-07-15 工作日志

## UI 设计：银保产品助手 H5 原型
- 需求：移动端 H5/小程序 + 保险产品展示对比 + 专业稳重金融蓝 + 面向银行渠道销售人员
- 产出：`index.html`（可交互高保真原型，手机外壳+4 Tab+详情抽屉+对比流程）、`design-system.md`（令牌/组件/响应式/无障碍）、`overview.md`
- 设计令牌：主蓝 #1A4DAB + 金 #C9A86A 点缀；4px 间距基准；纯 CSS 现金价值演示图（零图片）
- 路径：E:\workbuddyFIle\2026-07-15-00-06-48\

## UI 重做：喆少学习助手（zheshao-study 网站）
- 目标站点：`腾讯龙虾的成品\zheshao-study\`（GitHub Pages: https://1131921527-alt.github.io/zheshao-study/）
- 方案：金融蓝移动 H5 应用壳（同银保原型令牌）+ PWA 可安装（manifest + sw.js + icon.svg），保留原 4 板块外链，新增「中国历史」「通识地理」两个知识板块（站内可读文章）
- 关键点：`build.py` 每日自动重写 index.html → 改其首页输出为 `archive.html` 防覆盖；style.css 换金融蓝统一板块页风格
- 已 git commit+push 上线（合并冲突时 index.html/build.py 保留新版）。每日自动化仍可正常跑
- 华为/vivo：浏览器菜单「添加到主屏幕」即可当 App 用

## UI 改版 v2.1：深色黑金 + 财经科普/心理学
- index.html 整体换深色黑金令牌（--bg:#0B0F17 / surface:#151B2B / gold:#C9A86A / blue:#4F8DFF），金线描边、卡片悬浮微交互
- 新增「财经科普」(通胀/复利/股票基金) 与「心理学」(拖延症/当局者迷/多巴胺) 两个站内可读板块；首页 8 卡片
- 配图改为渐变横幅+emoji+金色光晕，点开可 Lightbox 放大
- manifest 主题色/背景改 #0B0F17，icon.svg 换黑金；已 commit+push (54e1424..2b52278)，raw.githubusercontent 已验证上线
- 已按用户选择「整站统一改深」：zheshao-study/assets/style.css 直接改黑金，并把 build.py 的 CSS 常量也换黑金（含 CLOCK_CSS 还原）
- 关键 bug：首次改 build.py 的正则 `CSS = """` 误匹配了 `CLOCK_CSS = """` 子串，导致 CLOCK_CSS 被塞进深色页面 CSS、真实 CSS 仍是浅色，write_css 生成浅色 style.css。用 repair_build.py 修正（精确匹配 CLOCK_CSS 与行首 ^CSS），两仓库均已修复并语法校验通过
- 两仓库(zheshao-study / 哲少的学习任务)都 push 到同一 remote(zheshao-study.git)；哲少的学习任务 是自动化生成工作区。已 commit+push 哲少的学习任务(a0da896) 使自动化基础为深色，live style.css 经 raw 验证为 #0B0F17 深色。整站黑金且每日自动生成保持深色
- **404修复**：用户报告 Pages 返回 404，排查确认文件完整、raw URL 正常，系 GitHub Pages Jekyll 处理干扰。添加 `.nojekyll` 文件推送（commit `5fbfecd`）解决
- **点击失效根因修复**：用户反馈PWA装到桌面后点卡片无反应。根因是 index.html 的 JS 数据里 5 篇文章标题用了半角英文双引号（"呼吸" "变毛" "生小钱" "情绪管理" "陷阱"），截断 JS 字符串字面量，导致整段 script 语法错误、所有 onclick 函数 undefined，点啥都没反应。用全角中文引号替换修复（node --check 验证通过）。另：sw.js 原为 cache-first 会一直喂旧缓存，改为 HTML network-first + 静态 cache-first，缓存名升 v2 强制刷新。首页4张NEW卡改为 goKnow() 直达并展开对应知识板块。commit `75175f2`

## 长文内容深化（混子哥风格）+ 部署解锁
- 用户需求：知识点不能是单张图片/卡片，要「很长的一篇可滚动精讲」（参考混子哥边讲边画，历史/地理/财经/心理都要长文）
- 做法：用 gen_articles.py 生成 12 篇深色黑金长文阅读页（历史3：文明曙光/秦汉/唐宋；地理3：板块/河流/雨林；财经3：通胀/复利/股票基金；心理3：拖延症/旁观者效应/多巴胺），每篇 540–1150 字、含分章标题/重点callout/图解块/时间线/对比表
- **关键决策**：知识子页 knowledge/*.html 在 Pages 上 404（同目录 article.css 正常），判定为 Pages 增量构建卡住。改为把 12 篇长文直接内联为 `<section class="screen" id="art-<slug>">` 内置可读屏，删掉独立 html，renderK() 动态生成 `onclick="goArt('art-<slug>')"` 跳转。goArt() 切换 .screen.active、返回条 `goTab('s-know')` 回知识中心。commit `041ba35`
- **部署卡死解锁**：push `041ba35` 后 live 仍停在旧 `b9d1b4b`（沙箱无法访问 GitHub API 手动触发构建）。移除 `.nojekyll`（commit `83daa09`）切换回 Jekyll 构建解卡 → live 恢复正常
- **最终验证通过**：WebFetch live 站确认含 knowledge/article.css 链接 + `function goArt` + `id="art-history-preqin"` 全篇长文；article.css raw 返回有效深色CSS（非404）；node --check 内联 script 无语法错误。12 屏齐全、导航闭环
- 现状：https://1131921527-alt.github.io/zheshao-study/ 已上线，点首页知识卡→展开板块→点文章→长文滚读→返回，全程站内无跳转死链

## 雅思单词完整版：加载即乱序
- 需求：用户打开 `学习资源/雅思词汇/html/ielts-vocab-full.html`（完整版，89词）每次都从同一词（abundant，用户误记abandon）开始，要「每次点进来乱序」
- 结构：`<div class="wrap">` 内含 89 个 `<div class="word-card">`（硬编码HTML，非JS渲染），脚本仅在末尾处理音频播放
- 改动：① hero 区加 `🔀 重新洗牌` 按钮（`.shuffle-btn` 样式沿用页面蓝灰）；② 脚本加 `shuffleWords()` 用 Fisher-Yates 打乱 `wrap.children` 顺序并 `appendChild` 重排，页面加载即调用 → 每次打开自动乱序；按钮 onclick 复用该函数
- 验证：node --check 内联 script 无语法错误；用 node 模拟确认 wrap 内恰 89 卡、原始前3=abundant/deteriorate/inevitable、打乱后变 empathy/hereditary/therapy、词数不变
- 提醒用户：若该完整版由每日自动化重新生成，改动可能被覆盖，需把乱序写进生成脚本才持久；待用户确认是否扩展至 day01/day13 等单日页

## 知识中心四大优化（用户截图反馈驱动）
- 起因：用户发知识中心截图，指出 4 点不满意
- **①去配图**：删除全部 13 个 `.figure` 图解块（渐变横幅+emoji+光晕，颜色太亮太丑），替换为纯文字 `.art-head` 标题栏（meta标签+大标题，金线分隔）
- **②AI板块移出知识中心**：删除 `s-know` 内的「AI系统性学习」「提示词技巧」两张 linkcard（属AI板块非知识类型）；topbar sub 改「历史·地理·财经·心理」，首页卡 qd 改「历史地理财经心理」
- **③12篇长文全面重写（混子哥风格）**：每篇从 ~1500 字扩写到 ~3500+ 字，口语化+比喻多+有故事有笑点，覆盖历史3/地理3/财经3/心理3
- **④展开/收起整行可点击**：4个板块标题加 `k-toggle` 类与 `onclick="toggleK(...)"`，点整条横幅任意处即可展开/收起（原只能点末尾「展开」两字）
- 已 commit `b04bdc0` 并 push（`83daa09..b04bdc0 main -> main`）；node --check 验证通过；figure 13→0、h-emoji 13→0、AI系统性学习 0、k-toggle 出现、art-head 12、文件 895→1565 行
- 待办：WebFetch 验证 live 站是否已反映 b04bdc0（之前有 Pages 增量构建卡住史，本次走 Jekyll 应正常）

## 内容扩充：+20篇长文（共32）+ 古诗词板块 + 雅思+100词（共189）
- 用户需求：雅思加100词并加载乱序（写成脚本持久化）；再生成20篇长文（历史/地理/财经/心理，心理多些）；新增古诗词板块；内容严谨但风趣、讲故事不失真、有趣味知识点；深色模式要干净有吸引力；做好分段；整行展开要刷新
- **知识中心 +20 篇长文**（gen_knowledge.py 生成并注入 index.html，幂等带 marker）：
  - hist4(魏晋风度/丝绸之路/罗马/文艺复兴) geo3(洋流/火山/沙漠) fin3(汇率/保险/资产配置) psy6(确认偏误/锚定/从众/沉没成本/曼德拉效应/邓宁-克鲁格) poem4(诗经/唐诗/宋词/苏轼)
  - 新增 `POEM` 数组 + SLUG.poem + renderK/openArticle 的 poem 映射；s-know 加「📜 古诗词」整行可点展开块
  - topbar sub 改「历史 · 地理 · 财经 · 心理 · 诗词」，首页 qd 改「历史地理财经心理诗词」
  - 共 32 屏（12 原 + 20 新），与旧文同 `.callout/.summary/.body` 结构，article.css 直接复用
- **雅思完整版 +100 词**（gen_ielts_full.py 生成并注入 学习资源/雅思词汇/html/ielts-vocab-full.html，再同步到 zheshao-study/ielts/full.html）：
  - 89→189 词（NEW100 带 MARKER 幂等注入）；新增 100 词无真人 mp3 → 加 `tts()` 浏览器语音兜底（playWord/playSentence 失败时调用 speechSynthesis）
  - 加载即乱序（shuffleWords Fisher-Yates 已在上轮加）；副标题说明「新词与例句支持浏览器朗读」；node --check 通过
  - 注：旧 89 卡 story-box/tip-box 含转义乱码（&lt;div...），系早期自动化 bug，本次未动；如需可单独清理
- **整行展开修复（关键）**：用户报"整行点展开没更新"。经查 b04bdc0 的 `k-toggle` 仅写在 CSS（`.section-title.k-toggle`），实际 5 个板块标题 div 都只把 `onclick` 放在内层 `<span class="more">` 上，整行并不可点 → 之前所谓"已上线"是误判。本次把 `onclick="toggleK('x')"` 移到 `.section-title` div 本身并加 `k-toggle` 类（5 行全改），移除外层 span 上的重复 onclick 避免冒泡 double-toggle；sw.js CACHE 升 v3→v4 强制刷新。commit `325b9cd`
- 注：之前把整行展开说成"已在 b04bdc0 上线、只是 SW 缓存卡住"是不准确的，实际是 handler 没挂到整行；现已真正修好
- **深色模式复查**：knowledge/article.css 已是深黑金（#0B0F17 / surface #151B2B / gold #C9A86A），行高 1.95、无亮色配图，判定干净不刺眼，未改避免回归
- 已 commit+push：`338eb5c`（index.html/ielts/full.html/sw.js 内容）与 `cfd854e`（scripts/gen_knowledge.py + scripts/gen_ielts_full.py 入库，保证自动化/重跑可复现）
- raw GitHub 验证：index.html 含 klist-poem=1、art-poem-sushi、art-psy-dunning、const POEM、topbar 五词、32 屏；ielts/full.html 189 卡 + tts 函数
- 可选跟进：①清理旧 89 卡乱码 story-box；②day01–day17 单日页是否也加乱序

## 新增「情商沟通」板块 +5篇 (gen_eq.py)
- 用户要 5 篇高情商/如何讲话/职场心理学。新建独立板块「💬 情商沟通」（与认知偏差类「心理学」区分），脚本 scripts/gen_eq.py 注入 index.html（幂等，7处协同改动）
- 5篇：eq-basics(高情商本质)/eq-nvc(非暴力沟通)/eq-upmanage(向上管理)/eq-boundary(边界感与情绪劳动)/eq-icebreak(闲聊破冰)；混子哥风、含 lead/h2/callout/callout.blue/summary，约1800-2400字
- 7处协同改动（复用 gen_knowledge.py 模式）：①抽屉前插5个 `<section class="screen" id="art-eq-...">`；②`const SLUG={` 前插 `const EQ=[...]`；③SLUG 加 `eq:[...]`；④renderK `const maps` 加 `eq:EQ`；⑤openArticle 字面量加 `eq:EQ`；⑥s-know poem 后插「情商沟通」k-toggle 折叠块(`id="klist-eq"`)；⑦顶栏sub+首页qd加「情商」(现6板块)
- 顶栏现：历史 · 地理 · 财经 · 心理 · 诗词 · 情商
- **踩坑(重要)**：SLUG slug 别加双重前缀！ARTICLES.slug 已是 "eq-basics"，脚本拼 SLUG 时用 `"%s" % slug`（得 "eq-basics"）；若写 `"eq-%s"` 会变成 "eq-eq-basics"，而 section id 是 art-eq-basics → 点击打开不存在的屏。已修正并验证 SLUG.eq 与 section id 全配对
- 校验：node --check 内联JS；grep 数 art section=37、klist-eq=1、const EQ=1；python 配对 SLUG.eq 与 art-<slug> 无缺失
- SW CACHE v4→v5 强制刷新；commit b6fd151，HEAD 37屏/EQ5篇/v5 全验证

## 可复用：知识中心长文注入配方（沉淀 skill）
向 zheshao-study/index.html 加新板块/批量长文，固定 7 步，务必带幂等 marker 防重跑重复注入：
1. 文章 section 插在 `    <!-- 抽屉 -->` 前；marker `<!-- XX-SECTIONS -->`
2. 新 JS 数组 `const XX=[...]` 插在 `const SLUG={` 前；marker `const XX=[`
3. SLUG 对象加 `xx:[...]`（slug 必须与 section id 后缀一致，别加双重前缀！）
4. renderK `const maps={...}` 加 `xx:XX`
5. openArticle `({...})` 字面量加 `xx:XX`
6. s-know 板块区：在对应 `klist-<prev>` 后插 `<div class="section-title k-toggle" onclick="toggleK('xx')">…` + `<div class="klist hidden" id="klist-xx">`
7. 顶栏 sub(行216) 与首页 qd(行180) 补板块名
校验：node --check；grep 计数；python 配对 SLUG.xx↔art-<slug>；改 index.html 则 SW CACHE+1 强制刷新；commit+push 后 git grep HEAD 复验

## 本轮收尾：每板块+3篇(共55) · 雅思289词 · AI独立+知识类5类目重设计
- 用户一句话三件事：①每个知识板块再+3篇（心理要"名师讲课风"、情商要"职场关系学"）；②雅思再+100词；③重新分类——AI 独立成板块，历史/文化·文学/地理/财经/心理·情商 归「知识类」统管，年轻人一眼看清
- **Task#23 +18篇长文**（scripts/gen_more3.py，幂等带 `/*MORE3:<sec>*/` / `<!-- GEN-MORE3 -->` marker）：
  - hist3(文化史/大航海/工业革命) geo3(气候/人口/城市) fin3(利率/税/加密) psy3(恐惧/习惯/关系·名师讲课风"同学们") poem3(楚辞/李清照/元曲) eq3(职场关系/反馈/冲突)
  - 顺带修掉历史遗留孤儿 section：原 hist-/geo-/fin- 前缀多出的 7+3+3 个 `<section>` 不在 SLUG 里不可达，本次重写 SLUG 把全部 55 个 slug 纳入 → 0 孤儿
  - 现 55 屏：hist10/geo9/fin9/psy12/poem7/eq8
- **Task#24 雅思+100词**（scripts/gen_ielts_plus100.py，MARKER=`<!-- IELTS+100 -->`）：189→289，去重不与旧 189 重复；SRC=zheshao-study/ielts/full.html 与 DST=学习资源/雅思词汇/html/ielts-vocab-full.html 双写同步；新词 tts() 浏览器朗读兜底；计数文案改"共 289 词"
- **Task#25 重分类重设计**（按用户确认"知识类统管"）：首页三分组「🤖 AI 板块 / 📚 知识类 / 🛠️ 学习工具」；AI 独立蓝渐变卡 `.qcard.ai`；知识类 5 张子卡(历史/文化·文学/地理/财经/心理·情商)，心理·情商合并用一个折叠块 `toggleKPsyEq()` 展开 psy+eq 两个 klist(各带 .ksub 小标"心理学"/"情商沟通")，首页 `goKnowPsyEq()` 直达
- **踩坑(已修，重要)**：①`const EQ=[...]` 是单行数组，gen 注入的 `];` 行匹配器只认独立 `];` 行 → EQ 找不到；改为 `if "];" in line` 并加单行内联插入分支(`line.replace("];", new+"];",1)`)；②EXISTING["geo"] 顺序曾写反(volcano/desert)导致两篇文章点开串台，改正为 volcano 在前；③gen_ielts_plus100 首跑漏加 IELTS+100 marker（重跑会双倍注入），已补 marker 并 python 回补两文件；④git grep 验证语法：`git grep <pattern> <commit> -- <path>`，把 HEAD 当 pattern 会得 0（已改正）
- **校验全过**：python 配对 55 SLUG.slug ↔ 55 art-<slug> 零孤儿；node --check 内联JS 通过；git grep HEAD 复验 SLUG=55/art=55/word-card=289(精确，290含1条CSS)/CACHE v6/首页"6 个知识类目"
- 已 commit+push：`508ffc3`（5文件，+3086行；新增 gen_more3.py、gen_ielts_plus100.py）；SW CACHE v5→v6 强制刷新；live=https://1131921527-alt.github.io/zheshao-study/

## 收尾：全站品牌改名 喆少→泽少（用户级记忆补齐）
- 背景：用户嫌"喆"难打，要求"把整个都改成泽少的学习网站"（泽=王铭泽的泽）。网站仓库(67文件)与腾讯龙虾的成品/memory/MEMORY.md 此前已改完并 push（commit 9a2156a，含 time goes by + SW v8）
- 本轮补齐用户级身份文件，确保跨号/跨层一致：
  - `~/.workbuddy/USER.md`：Name 与 What-to-call-them 的"喆少"→"泽少"
  - `~/.workbuddy/MEMORY.md`：标题"# 喆少 · 全局记忆"→"# 泽少 · 全局记忆"；核心身份行"喆少（王老板）"→"泽少（王老板）"
- 验证：两文件 grep 喆少=0；zheshao-study 仓库本地+远程均 9a2156a、git status clean
- 结论：泽少品牌全量改名完成（站库 + 项目记忆 + 用户级记忆三层一致）。仓库名仍保留 zheshao-study（GitHub 不允许随意改名，URL 不变）
