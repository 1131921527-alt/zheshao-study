# 喆少学习助手 · 深色黑金 H5 改版概览（v2.1）

## 做了什么
把 `1131921527-alt.github.io/zheshao-study/` 这个学习站，从金融蓝风**整体升级为深色黑金（黑金/蓝金黑）移动 H5 应用壳 + 可安装 PWA**，并按你的要求新增了「财经科普」「心理学」两个知识板块，配图可点开放大。已 push 上线。

## 这个版本（v2.1）相比之前
- **配色整站换深**：底色 `#0B0F17`、卡面 `#151B2B`、金色 `#C9A86A` 做描边与强调、蓝色 `#4F8DFF` 做点缀；卡片悬浮微交互、金线分隔，整体"高级感"。
- **新增两大板块（站内可读）**：
  - 财经科普：钱为什么会变毛（通胀）、复利、股票与基金入门
  - 心理学：拖延症、当局者迷、多巴胺
- **配图可放大**：每篇文章的封面是渐变横幅 + emoji + 金色光晕的纯 CSS 图，**点一下弹出 Lightbox 大图**，关掉即可继续读。
- 首页变 8 张卡片（AI动态播报 / 知识中心 / 雅思单词 / 中国历史 / 通识地理 / 财经科普 / 心理学 / 我的），后两个带 NEW 标。
- PWA 主题色、图标同步换黑金，装到桌面更统一。

## 界面结构（底部 5 Tab）
1. **首页**：问候 + 数据概览 + 8 大板块入口 + 安装引导 + 打赏区
2. **动态**：AI动态播报（外链板块页）
3. **知识**：AI系统性学习 / 提示词技巧（外链）+ 中国历史 / 通识地理 / 财经科普 / 心理学（站内可读）
4. **雅思**：雅思单词（外链板块页）
5. **我的**：安装指引 / 加微信 / 打赏 / 关于

## 技术要点
- 深色黑金设计令牌：`--bg:#0B0F17` / `--surface:#151B2B` / `--gold:#C9A86A` / `--blue:#4F8DFF`，金线 `rgba(201,168,106,.16)` 描边。
- `manifest.webmanifest`（主题色 `#0B0F17`）+ `sw.js` 实现可安装 + 离线缓存；`assets/icon.svg` 黑金图标。
- 文章配图纯 CSS（渐变 + emoji + 金色光晕），**零图片、可离线、点击 Lightbox 放大**。
- 旧版生成器 `build.py` 仍把旧首页写进 `archive.html`，新版首页不会被覆盖；每日自动化照常跑。

## 交付 / 上线
- 已 `git push` 至 `1131921527-alt/zheshao-study` main 分支（commit `2b52278`），GitHub Pages 约 1 分钟生效。
- 文件路径：`腾讯龙虾的成品\zheshao-study\`（index.html / manifest.webmanifest / sw.js / assets/icon.svg / build.py）
- 验证：raw.githubusercontent 已确认含 `0B0F17` / `财经科普` / `心理学` / `lightbox` / `v2.1`。手机上若看到旧版，强刷一下即可。

## 子页配色（已按你选择「整站统一改深」完成）
- `zheshao-study/assets/style.css` 已直接换黑金；`build.py` 生成管道（真实 CSS 常量 + CLOCK_CSS 还原）也统一黑金，已 commit+push（哲少的学习任务 `a0da896`），每日自动生成出来的子页同样是深色。
- 修复一个生成器 bug：早先改 `build.py` 的正则误匹配 `CLOCK_CSS`，已用精确匹配修好，语法校验通过。
- 验证：raw 线上 `assets/style.css` 含 `--bg:#0B0F17`，整站黑金且持久。

## 你可以怎么用
- 手机浏览器打开站点 → 菜单「添加到主屏幕」→ 桌面出现图标，点开就是 App（全屏、可离线）。
- 想继续加文章或新板块，给我方向就行。
